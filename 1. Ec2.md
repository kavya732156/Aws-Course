***EC2 : (Elastic Compute Cloud)***
``` text 
EC2 is a core AWS compute service that offers on-demand, resizable virtual instances with configurable CPU,
memory, storage, and networking for running workloads.
```
***To create an EC2 instance, the following components are needed:***

```text
1. AWS Account – To access the AWS Management Console or CLI.
2. AMI (Amazon Machine Image)w – Defines the operating system and preinstalled software.
3. Instance Type – Specifies CPU, memory, and networking capacity.
4. Key Pair – Used for secure SSH/RDP access to the instance.
5. Security Group – Acts as a virtual firewall to control inbound and outbound traffic.
6. VPC and Subnet – Determines the network where the instance is launched.
7. Storage (EBS) – Provides persistent block storage for the instance.
8. IAM Role (Optional) – Allows EC2 to access other AWS services securely.
```
***Exercise 1***
1. Launch Linux Instance in N.Virgenia Region (Use Amazon Linux AMI)
2. Connect to the Instance By using ec2 Instance Connect and Create Some Files and Directories.
3. Connect to the Instance By using Mobaxterm and verify Files and Directories.

***Exercise 2*** 
1. Download and Install PUTTY and Mobaxterm Tools for Remote Connections...
2. Launch the Ubuntu Server by Pem format key pair, Connect by using Mobaxterm.
3. Launch the Windows Server and connect by using the Remote Desktop Connection App and Mobaxterm.
4. Launch the Redhat Server using ppk keypair and connect through Putty.
5. Convert the Key pair Format from Pem to PPK Format and PPK format to Pem Format.


